const camisaAzul = true; //boolean
const camisaPreta = false;

//10 > 5 = true

const numero = 11;
const numeroPar = (numero % 2) === 0;

// == ignora o tipo / === usa o tipo expecifico

if (numeroPar)
{
    console.log('numero Par');
}
else
{
    console.log('numero Impar');
}
